<Window x:Class="ZeroAccel.AboutWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
  Title="{DynamicResource S_About}" Width="360" SizeToContent="Height"
  WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
  Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}"
  FontFamily="Segoe UI" UseLayoutRounding="True" SnapsToDevicePixels="True">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="0" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
  </shell:WindowChrome.WindowChrome>
  <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="24">
    <StackPanel>
      <TextBlock Text="{DynamicResource S_About}" FontSize="11" Foreground="{DynamicResource MutedBrush}"/>
      <TextBlock x:Name="VersionLabel" FontSize="24" FontWeight="SemiBold" Margin="0,16,0,16"/>
      <TextBlock x:Name="EngineVersionLabel" FontSize="12" TextWrapping="Wrap"/>
      <TextBlock x:Name="DriverVersionLabel" Text="{DynamicResource S_AboutDriver}" FontSize="12" Margin="0,6,0,0"/>
      <TextBlock Text="{DynamicResource S_AboutDescription}" TextWrapping="Wrap" Foreground="{DynamicResource MutedBrush}" FontSize="12" Margin="0,16,0,0"/>
      <Grid Margin="0,24,0,0">
        <TextBlock Text="MIT · open source" FontSize="11" Foreground="{DynamicResource SubtleBrush}" VerticalAlignment="Center"/>
        <Button x:Name="DismissButton" Content="{DynamicResource S_Dismiss}" IsCancel="True" HorizontalAlignment="Right" Click="DismissClicked"/>
      </Grid>
    </StackPanel>
  </Border>
</Window>
